# Mateus Alves - Dotfiles

## How to use:

1. Clonar o repositório:
```bash
# SSH
git@github.com:alvesmateus63/dotfiles.git

# HTTPS
git clone https://github.com/alvesmateus63/dotfiles.git
```

2. Criar os links
```bash
ln -s ~/.dotfiles/.zshrc ~/.zshrc
ln -s ~/.dotfiles/.gitconfig ~/.gitconfig
```
